configuration {
    modi: "drun,run,window";
    show-icons: true;
    display-drun: "APPS";
    drun-display-format: "{name}";
    font: "IosevkaTerm Nerd Font 12";
}

@theme "/dev/null" /* This line forces Rofi to drop all default themes */

* {
    /* Nuanced Palette */
    bg: #0f0f0f;
    bg-alt: #1a1a1a;
    fg: #e0e0e0;
    accent: #a0a0a0;
    border: #333333;
    
    background-color: @bg;
    text-color: @fg;
    margin: 0;
    padding: 0;
    spacing: 0;
}

window {
    width: 450px;
    border: 1px;
    border-color: @border;
    border-radius: 12px;
}

mainbox {
    children: [inputbar, listview];
}

inputbar {
    background-color: @bg-alt;
    children: [prompt, entry];
    padding: 12px;
}

prompt {
    background-color: transparent;
    text-color: @accent;
    padding: 0 10px;
}

entry {
    background-color: transparent;
}

listview {
    padding: 10px;
    lines: 8;
}

element {
    padding: 8px;
    background-color: transparent;
}

element selected {
    background-color: @border; /* Subtle slate gray highlight */
    border-radius: 6px;
}

element-icon {
    size: 24px;
    padding: 0 10px 0 0;
}

element-text {
    vertical-align: 0.5;
    text-color: inherit;
}
